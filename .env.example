# FastAPI Server Config
PORT=8000

# Twilio Credentials (Optional: required only if Twilio "Secure Media" is enabled)
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here

# WhatsApp Demo Settings for Instant Customer Validation
DEBUG_CLIENT_NUMBER=+41780000000
# TWILIO_CONTENT_SID=HX... # (Optional) If you have a predefined Twilio WhatsApp Quick Reply template

# Gemini AI Config
# Optional for cart-to-devis extraction. If absent, /api/generate-devis uses
# deterministic regex/text heuristics and still returns a devis with warnings.
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-3.1-flash-lite

# Cart-to-devis security note:
# Supplier authentication is manual in the browser. Do not add supplier
# usernames, passwords, cookies, or session tokens to this file.

# Optional bexio draft connector.
# The Chrome extension never receives this token. Live creation is skipped unless
# the backend receives bexio_dry_run=false, X-Connector-Token matches this value,
# and all installation-specific IDs are set.
ACCOUNTING_CONNECTOR_TOKEN=
BEXIO_ACCESS_TOKEN=your_bexio_oauth_access_token_here
BEXIO_CONTACT_ID=
BEXIO_USER_ID=
BEXIO_BANK_ACCOUNT_ID=
BEXIO_CURRENCY_ID=
BEXIO_PAYMENT_TYPE_ID=
BEXIO_LANGUAGE_ID=
BEXIO_ACCOUNT_ID=
BEXIO_TAX_ID=
BEXIO_UNIT_ID=

# Optional Winbiz DocumentImport export. These values are installation-specific;
# leave them empty until confirmed in the garage's Winbiz setup.
WINBIZ_ADDRESS_CODE=
WINBIZ_COLLECTIVE_ACCOUNT=
WINBIZ_SALES_ACCOUNT=
WINBIZ_DOCUMENT_TYPE=10
WINBIZ_VAT_RATE=8.1

# Google Drive Parent Folder ID (ID of the "Garage_Dossiers" folder)
GOOGLE_DRIVE_PARENT_FOLDER_ID=your_google_drive_parent_folder_id_here

# Google Authentication Config
# To bypass the "storageQuotaExceeded" error for personal @gmail.com accounts:
# Use Option A (OAuth 2.0 User Credentials). If you are using Google Workspace with Shared Drives, you can use Option B.

# Option A: Google OAuth 2.0 User Credentials (Acts as your personal account with full quota)
GOOGLE_REFRESH_TOKEN=your_oauth_refresh_token_here
GOOGLE_CLIENT_ID=your_oauth_client_id_here
GOOGLE_CLIENT_SECRET=your_oauth_client_secret_here

# Option B: Google Service Account Credentials (Requires Shared Drives on Google Workspace)
# GOOGLE_SERVICE_ACCOUNT_JSON=./credentials.json
# GOOGLE_SERVICE_ACCOUNT_JSON={"type": "service_account", ...}
